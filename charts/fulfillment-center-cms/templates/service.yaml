apiVersion: v1
kind: Service
metadata:
  name: {{ include "fulfillment-center-cms.fullname" . }}
  labels:
    {{- include "fulfillment-center-cms.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "fulfillment-center-cms.selectorLabels" . | nindent 4 }}
