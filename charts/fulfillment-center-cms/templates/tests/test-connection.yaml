apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "fulfillment-center-cms.fullname" . }}-test-connection"
  labels:
    {{- include "fulfillment-center-cms.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "fulfillment-center-cms.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
